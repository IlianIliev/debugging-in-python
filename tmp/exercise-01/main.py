def append(item, container=[]):
    container.append(item)
    return container


default_user_settings = {
    "theme": "dark",
    "notifications": True,
    "language": "en",
}

def create_user(email, name):
    pass
